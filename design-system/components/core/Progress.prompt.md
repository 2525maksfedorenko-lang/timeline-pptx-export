Use `Progress` for project completion.

```jsx
<Progress value={64} style={{ height: 8, width: "60%" }} />
```

Notes: the projects table pairs it with a right-aligned `%` label in muted text at 14px and overrides the height to 8px (`h-2`); mobile cards use 6px.
