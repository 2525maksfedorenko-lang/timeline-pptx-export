Use `Popover` to host small forms and pickers (date picker, tag search, filter builder rows).

```jsx
<Popover trigger={<Button variant="outline">Filters</Button>} align="start">
  <Label>Owner</Label>
</Popover>
```
