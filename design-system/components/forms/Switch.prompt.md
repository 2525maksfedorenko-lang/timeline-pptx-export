Use `Switch` for instant-apply booleans (assistant active, automated mails, sharing defaults).

```jsx
<div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
  <Label>Assistant active</Label><Switch defaultChecked />
</div>
```
