Use `Checkbox` for multi-select rows, permission matrices and table selection.

```jsx
<div style={{ display: "flex", alignItems: "center", gap: 8 }}>
  <Checkbox defaultChecked /><Label>Send automated mails</Label>
</div>
```
